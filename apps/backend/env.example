## Required
OPENAI_API_KEY=
LANGGRAPH_PG_URL=postgresql://user:pass@host:5432/db

## Supabase auth (backend validates JWTs)
# Required to fetch JWKS (sent as `apikey` header)
SUPABASE_ANON_KEY=
# Optional: if set, backend will derive JWKS URL automatically.
SUPABASE_URL=https://<project-ref>.supabase.co
# Optional override (otherwise derived from SUPABASE_URL)
SUPABASE_JWKS_URL=https://<project-ref>.supabase.co/auth/v1/.well-known/jwks.json
# Recommended for HS256 access tokens (fallback if JWKS is unavailable or token is HS256)
SUPABASE_JWT_SECRET=

## Optional enrichment
TAVILY_API_KEY=
GITHUB_TOKEN=

## Optional (used by some nodes/admin flows)
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_KB_TABLE=knowledge_base

## Observability (optional - Sentry)
# Create a project in Sentry and paste its DSN here to enable error/perf reporting.
SENTRY_DSN=
# e.g. production, staging, development
SENTRY_ENVIRONMENT=development
# Optional release identifier (git SHA, semver, etc)
SENTRY_RELEASE=
# Perf sampling (0.0 - 1.0)
SENTRY_TRACES_SAMPLE_RATE=0.05
# Profiling sampling (0.0 - 1.0). Keep 0.0 unless you explicitly want profiling.
SENTRY_PROFILES_SAMPLE_RATE=0.0


